name: multiomics_robustness
channels:
  - defaults
dependencies:
  - python=3.11.7
  - pip
  - pip:
      - numpy==1.26.4
      - pandas==2.2.2
      - scipy==1.13.1
      - scikit-learn==1.5.2
      - matplotlib==3.8.4
      - anndata==0.11.4
      - scanpy==1.10.3
      - h5py==3.11.0
      - PyYAML==6.0.2
      - tqdm==4.66.5
      - mudata==0.3.10
      - torch==2.14.0
      - scvi-tools==1.3.3
# Recreate:
#   conda create -n multiomics_robustness python=3.11.7 pip -y
#   conda activate multiomics_robustness
#   pip install -r requirements.txt
# Confirmed on macOS 14.6.1, Apple M1 Pro, no CUDA, MPS available.
# Full pinned tree is in requirements.txt.
